# Progress log

Workstreams started 2026-08-26 (lead session, subagents coordinated via `.agent-bus/`).

| # | Workstream | Plan | Status |
|---|-----------|------|--------|
| A | Cards blurry on 4k/HiDPI screens when zoomed in | docs/plans/A-hidpi-cards.md | done |
| B | Synthetic tax / financial / customer-service datasets | docs/plans/B-datasets.md | landed: tax-cases(+geo), tax-returns, payments; Titanic removed; faker names |
| C | First-visit onboarding walkthrough with ElevenLabs voiceover | docs/plans/C-onboarding.md | done (16-step story, anchored caption, e2e green); audio blocked on key permission (see log) |
| E | Customer-journey flagship: UK map (lat/lon), rich customer card, detailed record view, revised tour, female Scottish voice | docs/plans/E-customer-journey.md | done: E1 map/lights, E2 card, E3 detail all landed |
| G | Performance pass (Fable), after everything else | — | in progress |
| F | Category colours: pinned per-category colours (`Dataset.colors`) + auto-detected colour-name labels | (user request, no plan doc) | landed: src/core/palette.ts, columnar.ts, pixels.ts, app.ts/main.ts/facets.ts swatches, tests/palette.test.ts |
| D | Full code review + remediation | docs/plans/D-code-review.md | all 8 groups landed |
| I | Cards: one record per card, a quieter face, an expanded modal | docs/plans/I-cards.md | WP1–WP6 landed |

## Log
- 2026-08-26: kicked off; planners writing plans in parallel.
- 2026-08-26 23:30: plan A written; implementer A started (atlas/renderer/hires).
- 2026-08-26 23:33: plans B and C written (C reconciled to B's D1 schema; tour dataset tax-cases:900); implementers B and C started in parallel.
- 2026-08-26 23:37: code review plan D written (50 findings, 8 parallel groups). Top: pixel raster distorts under filter, drag-pan ends in click-select, DPR>2 camera cap, worker errors swallowed, dataset load race.
- 2026-08-26 23:45: A implemented. Base atlas slot now sized per collection (`slotFor`: 891 → 128 px, ≤ 8 templates → 1024); a lazily allocated second hi-res atlas (`u_hi`, `a_meta.w` flag) re-rasterises the cards in view on camera settle at `min(1024, nextPow2(cardPx))`, ≤ 24 cards/tick, still one draw call. DPR pointer cap dropped. `perItem` threshold moved from ~950 to 3136 rows (the 64 px slot's real capacity with 4 px pad — the plan's 3844 assumed no pad), so 1000–3136-row collections now get per-item cards. Verification `node scripts/verify-hidpi.mjs`: tier 512 engaged at DPR 2 and 1; centre-400px gradient *energy* 3.4x vs `?hires=0`. The plan's mean-|gradient| metric only reaches 1.26x — it is a total-variation measure that an isolated edge keeps regardless of blur — so the script asserts on gradient energy and prints both. Screenshot `screenshots/hidpi-zoomed.png`. 13 new tests in tests/atlas.test.ts; suite at 131 + typecheck pass.
- 2026-08-26 22:45: user added workstream E (map + card + detail view + revised tour, Scottish female voice). impl-B redirected to add geography/demographics; impl-C told to hold audio and switch voice. Planner E started.
- 2026-08-26 23:45 (impl-C): tour engine/player/UI/host/actions built under src/tour (data-driven from script.ts); #tourBtn, facets toggle/clearAll, tests (engine, script hash, columns vs tax-cases) green; scripts/generate-voiceover.mjs with --dry-run/--force/--only/--list-voices/--add-voice; Playwright e2e scripts/tour-e2e.mjs on port 5182.
- 2026-08-26 23:45 (impl-C): scope change from user — no audio generated yet; voice switched to a female Scottish library voice (Isla Skye, TVmbglAk3F1GkiCoOq47; needs `pnpm voiceover --add-voice` once). Narration/steps to be swapped from docs/plans/E-customer-journey.md when it lands.
- 2026-08-26 23:46: user feedback — Titanic uses real victims' names: impl-B told to remove it and use seeded faker en_GB names; B plan amended with the geo/demographic addendum (had been sent to impl-B by message only).
- 2026-08-26 23:47: lead verified tax-cases geo in the browser: Scatter opens as Longitude×Longitude (black) — pre-existing default-axis bug at main.ts:179/76-77; handed to plan E (map default). Screenshot screenshots/map-20k-pre-E.png.
- 2026-08-26 23:37: B — shared PRNG (src/data/random.ts), tax-cases D1 + registry (src/data/registry.ts) landed; app.ts loadByKey → resolveDataset; main.ts menu from menuEntries() with optgroups; announced on the bus for C.
- 2026-08-26 23:43: B — tax-returns, payments landed; tax-cases geo/demographic addendum (src/data/ukPlaces.ts, Customer/Postcode/Town/Age band/Area type/Longitude/Latitude); invoices (D4) landed.
- 2026-08-26 23:55: B — Titanic removed per user feedback (src/data/titanic.ts, csv.ts deleted; bench small target → tax-cases:900; default collection tax-cases:5000). Names via @faker-js/faker en_GB, dynamic-imported: main bundle 39 kB gzip, faker chunk 157 kB gzip loaded only for ≤50k tax-cases / invoices. main.ts: raw Scatter no longer opens on the same column for both axes. Tests 163 pass; typecheck clean except tests/tour-script.test.ts (C: needs node types). Not done: scripts/export-csv.ts (no vite-node in node_modules/.bin).
- 2026-08-26 23:51: B: Titanic removed (real names), tax-cases:5000 default, faker en_GB names via dynamic import (157 kB gz chunk).
- 2026-08-26 23:55: plan E written (635 lines). Lead decision: default + tour dataset = tax-cases:3000 (per-item cards; 5000 was category mode). Started impl-E1 (map/lights) and impl-E3 (detail view); E2 (card) queued behind E1's app.ts change; B doing kind/geo/labelColumn/sizes; C implementing revised narration, audio after E3.
- 2026-08-26 23:58: B — E-plan asks landed: Dataset.kind/geo fields (columnar.ts), tax-cases sets kind + geo, labelColumn = Customer when names exist, sizes [900, 3000, 20000, 100000], default collection tax-cases:3000 (per-row cards under the atlas cap).
- 2026-08-26 23:53: B complete incl. E asks: Dataset.kind/geo, labelColumn=Customer, sizes 900/3000/20000/100000, default tax-cases:3000.
- 2026-08-26 23:54: E1 app.ts change in; impl-E2 (flagship customer card, Fable) started.
- 2026-08-26 23:58 (impl-C): tour revised to plan E §4 (16 steps map→city→grid→facet→sort→colour→bars→area→crosstab→back→record→card→detail→fit→finish; TOUR_DATASET tax-cases:3000; rowWhere/worldOf/cardZoom helpers; UI bold-term set). vitest 166 green, typecheck clean, tour e2e all passes on the current tree. Audio still held for E3's detail view.
- 2026-08-26 23:58 (impl-E3): E3 detail view landed. `src/core/esc.ts` (shared HTML escaper; facets.ts imports it), `src/ui/detail.ts` (`DetailPane` owning `#detail`: `role="dialog" aria-labelledby="detailTitle"`, close button focused on open, registry `registerDetail(kind, renderer)`, generic renderer = old `showDetail` body with `esc()` — closes D-25/D-13/D-31 for this pane), `src/ui/detail/taxCase.ts` (customer record: header name + postcode · town · case ref; Customer / Case / Journey sections, priority chip, status dot, channel glyph, Opened → contacts → Resolved/still-open timeline, satisfaction stars; `Review action` / `Reassign` / `Add note` links with `href="#"` + `data-action`, preventDefault + stopPropagation, toast "Demo only — …"). main.ts: `showDetail` removed, `DetailPane` instantiated, Escape/close/dataset-change route through `detail.hide()` → `onClose` clears the selection ring. `#toast` is `role="status" aria-live="polite"`. CSS block appended (`.detail.rich` 360 px, scrollable). `node scripts/detail-e2e.mjs` (port 5195): 22 checks pass — dialog semantics, three links, no `framenavigated`/href change on click, toast text, Escape hides + ring byte cleared, generic `<dl>` pane on products:1000. Screenshot `screenshots/detail-customer.png`. Typecheck clean; only failing vitest is E1's in-flight `layouts.test.ts` bounds tolerance.
- 2026-08-27 00:05 (impl-C): user feedback received — (1) caption card must anchor to the spotlight with an arrow and be bolder; (2) narration to be an insight-led story verified against tax-cases:3000. Audio BLOCKED: API key lacks `add_voice_from_voice_library` (library voice Isla Skye cannot be added; premade voices have no Scottish accent). Resume point for another agent: src/tour/ui.ts (card anchoring), src/tour/script.ts + actions.ts (story), tests/tour-story.test.ts (data claims), then `pnpm voiceover --add-voice && pnpm voiceover` once the key permission is granted, then `pnpm test:e2e`.
- 2026-08-26 23:58: A bench regression found and fixed. `pnpm bench --swiftshader` could not complete twice (HMR reload from concurrent edits, then a Chromium crash on the 1M sweep), so the zoom phase was measured directly on a `vite preview` build at 900/1000 rows, hi-res on vs `?hires=0`: p95 117–150 ms vs 50–67 ms — the bench writes `cam.current` every frame so `camera.update` never reports motion and the hi-res pass rasterised during the sweep. Settle now also requires the camera to be unchanged since the previous tick (`hiLastCam`); zoom p95 is back to 33 ms both ways (noise). Verify script re-run clean at DPR 2 and 1 (tier 512, energy ratio 14–18x). Suite at 173 with other streams' tests.

- 2026-08-27 00:02: user feedback: tour caption must be bold and anchored next to its target; narration to be an insight-led story (sent to impl-C). Added F (category colours, impl-F started) and G (perf pass, queued). Alarm moved to 03:35. docs/HANDOFF.md written for resumption.
- 2026-08-27 03:36: lead resumed after the 03:35 alarm. E1/E2/F/C had been cut off by the session limit; resumed via message with their context. Tree: 174 tests pass; 1 typecheck error in C's in-progress tests/tour-story.test.ts. BLOCKER (needs Harry): ElevenLabs key lacks add_voice_from_voice_library — add 'Isla Skye' in the ElevenLabs UI or grant the permission.
- 2026-08-27 (impl-E1): E1 map layout + night lights landed. `{type:'xy', equal:true}` branch in src/layout/layouts.ts (`mapScale`, `MAP_SPAN`=240, `MAP_DOT`=0.7; column extents + `data.n`, so a facet filter never moves the map). Shaders: `u_lod` band and `u_glow`; output is premultiplied (`vec4(rgb*a, a)`, blend `ONE, ONE_MINUS_SRC_ALPHA`); under the LOD band on the map each card's quad grows into a halo (3x the card, >= 6 px) with a gaussian warm core emitted at alpha 0 (additive), so towns sum towards white while the card SDF stays at its true size inside the quad. Still one draw call. app.ts: `isMapView`, `renderer.lod = [14,32]` + `glow` on the map, `?glow=0`. main.ts: `equal` in currentSpec, Y != X guard, Y seeded with nums[1], geo default in load(). Tests: 7 equal-aspect cases in tests/layouts.test.ts; suite 173 pass. Verification `node scripts/verify-map.mjs --swiftshader` (port 5194) on tax-cases:20000: default spec {xy, Longitude, Latitude, equal:true}; bounds aspect 0.5332 = lon*cos(lat)/lat; dot 3.3 px at DPR 2 / 1.5 px at DPR 1; centre-60% mean luminance *above the clear colour* 0.0350 with glow vs 0.0114 with ?glow=0 = 3.08x (the plan's raw mean is dominated by the background and gives 1.36x, so the script subtracts it); 2.02 % of pixels above 0.8 luminance; Scatter tab gives Longitude x Latitude. Screenshots screenshots/map-dpr2.png, map-dpr1.png — the UK reads from the lights alone. Blend regression: tax-cases:900 grid frame (DPR 1, ?hires=0) with the old straight-alpha blend vs premultiplied: mean |diff| 0.0000/255, max 0, 0 of 5,205,792 channels differ (`--blend-capture/--blend-compare`; the capture waits for renderer.t >= 1 and the camera to settle, otherwise a cold server shifts the frame by a subpixel). The harness removes the vite type-checker overlay before screenshots (other workstreams' errors were covering the canvas).
- 2026-08-27 03:43: lead verified E1 map (UK legible from lights, 3.08x brightness, blend 0 diff), E2 card (zoomed + grid screenshots), F colours. D groups 3 and 1+5 started. Remaining: C (tour), D group 7 after C, review loop, G perf pass, README consolidation.
- 2026-08-27 (impl-D46): D groups 4+6 landed. Group 4: D-17/D-18 obsolete (csv.ts gone); D-30 single `PIXEL_TITLES` in src/data/pixels.ts, registry imports it; D-37 tests/helpers/prng.ts re-exports `mulberry32` from src/data/random.ts; D-38 `binNumeric` deleted from src/data/columnar.ts (fmtTick/shortNumber unification left as a note for the layout owner in D-code-review.md). Group 6: D-19 AxisOverlay.render early-returns when camera/viewport/axes unchanged (src/ui/axes.ts); D-29 FacetPanel builds DOM once, `update()` refreshes in place (focus preserved), D-35 "+N more" line (src/ui/facets.ts, .facet-more in style.css); D-23 Hud rows updated via text nodes, history() preallocated; D-39 hud.setGpu/axes.clear removed. New tests/facets.test.ts. typecheck clean, 221 tests pass. Resume: nothing half-done; Group 3 (app.ts) untouched.
- 2026-08-27 (impl-D3): D group 3 (src/app.ts core) landed. D-05 `loadSeq` guard in `loadDataset` (bails after resolve / engine.load / first setLayout when superseded) + id-keyed `LayoutEngine.load` (src/layout/client.ts + worker.ts, merged with impl-D15's D-04); D-09 `loadDataset(key, initial?)` with `app.defaultLayout()` (map for geo, picture for pixels, sorted grid otherwise — one solve on load); D-10 `app.pitch` from `LayoutSolution.pitch`, `scale = zoom * pitch`; D-12 done by B's registry fallback (noted); D-14 dead condition dropped; D-20 `SEQUENTIAL_RGB` precomputed; D-22 `toLayoutData` ships facets ∪ geo only (exported); D-27 `PivotApp.dispose()`; D-34 `REDUCED_MOTION` → `transitionMs` 0; D-06 device-pixel-content-box observer + `(resolution: Ndppx)` listener; D-02 consumer (`onClick` skips on `camera.wasDrag`); `fit()` no-op when `visible === 0`; D-46 app.ts comment. New tests/layout-client.test.ts (FakeWorker; D15 extended it). For Group 7: canvas needs `touch-action: none` in style.css; `main.ts load()` can drop the `apply(true)` re-solve after `loadDataset` (or pass `currentSpec()` as `initial`); `camera.focus`/`zoomTo` ms should honour reduced motion; README:71 10 Hz (lead). Also wired impl-D15's D-07/D-26 hooks: `renderer.onContextRestored` → `buildCards()` in the constructor; `buildCards` sizes the atlas from `renderer.atlasSize` (ATLAS_SIZE const removed). typecheck clean, 235 tests pass.
- 2026-08-27 03:50 (impl-C): caption card now anchors beside the spotlight (below/above/right/left, arrow, viewport flip, bolder type); narration rewritten as an insight-led story (map → channel colours → bars → area → age cross-tab → hours/satisfaction scatter → Post filter → +Open +High = 12 → grid by Contacts → zoom → featured customer's card → detail → clear → fit); every claim asserted in tests/tour-story.test.ts against tax-cases:3000. Engine exposes `phase`; e2e config disables HMR + checker overlay so concurrent edits cannot reload the page mid-run.
- AUDIO STILL PENDING (blocked on key permission `add_voice_from_voice_library`; voice Isla Skye TVmbglAk3F1GkiCoOq47). Command to run once the voice is in the workspace: `pnpm voiceover --add-voice && pnpm voiceover --dry-run && pnpm voiceover`, then commit public/audio/tour/. Tour degrades to captions + reading-pace timer meanwhile.
- 2026-08-27 03:49: D group 3 landed (235 tests). Group 7 notes carried: canvas touch-action:none; main.ts load() initial spec; camera focus/zoomTo reduced-motion; README 10 Hz line.
- 2026-08-27 03:50 (impl-D15): D groups 1+5 landed. Group 1: `src/layout/layouts.ts` (D-01 `isRasterGrid` from column extents + `data.n`, `LayoutResult.pitch`; D-36 integer-edge bins; D-38 `fmtTick` = `shortNumber`), `src/layout/worker.ts` + `client.ts` (D-04 error reply / reject / `onerror`, `pitch` on `LayoutSolution`; merged with impl-D3's id-keyed load), tests in `tests/layouts.test.ts` (7 new: D-01 raster under mask, non-raster fill, NaN/empty mask, D-36) and `tests/layout-client.test.ts` (3 new). Group 5: `src/gl/renderer.ts` (D-07 context lost/restored with `init()`/`reupload()`/`onContextRestored`; D-16 drain on disjoint; D-26 `maxTextureSize`/`atlasSize`; D-39 `jumpTo`/`dim`/`RenderStats` removed; D-21 note), `src/gl/shaders.ts` (`a_meta.z` spare), `src/gl/atlas.ts` (`CardAtlas.size`). impl-D3 owns the two app.ts hooks (`renderer.onContextRestored = () => { this.buildCards(); this.dirty = true; }`, `renderer.atlasSize` in `buildCards`, `scale` from `pitch`). Verified: `pnpm typecheck` + `pnpm test` (235 pass), `node scripts/verify-map.mjs --swiftshader` (3.08x, 2.02 %, all ok) and `node scripts/verify-hidpi.mjs --swiftshader` (6.24x / 6.25x, ok) unchanged from before.
- 2026-08-27 04:20 (impl-C): record step spotlights the featured card itself (world→screen rect, waits for the camera to settle); `pnpm test` 235 green, `pnpm typecheck` clean, `pnpm test:e2e` all passes (screenshots/tour-{area,facet2,record}.png). Done bar audio.
- 2026-08-27 03:53: C complete (insight-led 16-step tour, anchored bold caption — lead verified screenshots tour-facet2/tour-record; audio pending voice). D group 7 started; Fable review round 1 started (docs/plans/H-review-round1.md). Then: fix round, G perf pass, README consolidation.
- 2026-08-27 (impl-D7): D group 7 (src/main.ts, index.html, README) landed. D-24 try/catch in `load()` with select revert + toast, `loadSeq` guard, `apply()` toasts LayoutEngine rejections (D-04); D-25 `esc()` on menus, legend, bench report; D-09/D-34 Group 7 halves: `load()` uses `loadDataset(key)` once (no `apply(true)` re-solve) and `camera.focus` honours reduced motion via `renderer.transitionMs`; D-32 roving-tabindex tabs with arrow keys, `type="button"` everywhere; D-40 `OTHER.dark`; D-41; D-42; D-44 `<link>` dropped. D-11/D-13/D-31 and canvas `touch-action: none` were already satisfied by impl-C/impl-E3 — marked done. README: idle-redraw line corrected (idle frames are not drawn at all), test count → "the vitest suite". typecheck clean, 235 tests, tour-e2e + detail-e2e pass.
- 2026-08-27 03:56: D group 7 landed — all D remediation groups complete. Waiting on review round 1.
- 2026-08-27 04:02: review round 1 (3 High/11 Med/19 Low, all checks green). Fix groups 1–3 started in parallel. Next: G perf pass, README consolidation, final verification.
- 2026-08-27 (fix-1): H-review Group 1 landed — H-01 (`hiResKey` incl. solveSeq, `hiKey` reset in `setLayout`), M-01 (`LayoutResult.cardSize` → `app.cardSize`), M-08, M-11 (load reject on worker error), L-01/02/03/04/06/15, M-05 app side (`zoomStep(dir, ms)` honours reduced motion). Files: src/app.ts, src/gl/{hires,atlas,camera,renderer}.ts, src/gl/cards/taxCase.ts, src/core/ease.ts (new), src/layout/{layouts,worker,client}.ts, src/bench/bench.ts, tests/hires.test.ts (new), tests/layout-client.test.ts. typecheck clean, 242 tests, verify-hidpi/card/map green. Status column added to docs/plans/H-review-round1.md.
- 2026-08-27 04:07: fix group 1 landed (242 tests, verify scripts green).
- 2026-08-27 (fix-3): H-review Group 3 landed. M-10 confirmed (`.gitignore:7` `.env.*` ignores `.env.local`; commit `.gitignore` alone first). `src/core/palette.ts`: `colorOfRow(ds, colorBy, i, theme?)`, `hexToHsl`, `hasCoreHue` exported, private `mixHex` → `mixHueHsl` (L-01 for Group 1), M-06 core-hue gate + synonym collapse + neutrals use `OTHER[theme]`. `src/main.ts`: M-03 (`setMask` toast, menu `load().catch`), M-05 `tourHost.tweenMs`, L-07 `describeZoom` from `onLayout`, L-08 `selectDatasetOption`, L-19 legend swatches escaped. `src/data/registry.ts` L-13 comment / L-14 `load?`; `taxCases.ts` L-18 `isEscalated`; `scripts/generate-voiceover.mjs` M-09 (per-clip manifest, convert try/catch + `--add-voice` hint, `--only` unknown → exit 1); L-09 scripts use `tax-cases:900`/`:3000`; L-16 deploy.yml permissions on the deploy job; L-17 `@types/node` + plain imports in tests/helpers/nodefs.ts. Tests: tests/palette.test.ts (M-06 gate, synonyms, pairwise separation, light theme, colorOfRow incl. Tone pins), tests/registry.test.ts (no faker at tax-cases:100k, describeKey off-menu/unknown pixel image, pixels has no `load`). `main.ts` load guard not unit-tested (module constructs PivotApp/WebGL at import). Still open in this area: L-19 facets.ts side (ui, Group 2).
- 2026-08-27 04:08: fix group 3 landed (256 tests, build OK). Open: L-19 facets.ts side (ui). Waiting on fix group 2.
- 2026-08-27 (fix-2): review round 1 Group 2 (tour + detail) landed. H-02 tour keyboard map scoped to body/card (page controls keep native Enter/Space/arrows); H-03 `TourStep.run(signal)` — abortable `sleep`/`settle`, every action checks the signal before mutating the host; M-02 pane accent via `colorOfRow`; M-04 detail pane returns focus on hide; M-05 `TourHost.tweenMs?(ms)` + `cardZoom` from `app.cardSize`; M-07 audio ceiling timer; L-05 `src/tour/store.ts` (one `TOUR_KEY`/`safeStorage`/`markTourDone`), `markup` uses `core/esc`; L-10 rAF spotlight tracking for rect targets; L-11 empty-steps guard; L-12 `AudioPlayer.dispose()`. Found via new test: `shouldAutoStart` returned false with a null storage (fixed). New tests: tour-ui (jsdom, added as devDep), tour-index, detail, esc, plus engine abort/ceiling/dispose cases — suite 291 pass / 3 skipped, typecheck clean, tour-e2e (3 passes) + detail-e2e (22 checks) green.
- 2026-08-27 04:10: all review-round-1 fix groups landed (291 tests, e2e green). Started G perf pass (Fable) and README/HANDOFF consolidation.
- 2026-08-27 (docs): README.md consolidated into one document verified against the source (registry sizes, tour script, hi-res atlas, map/night-lights, palette, detail view, scripts, deploy workflow); every Titanic mention removed from docs; scripts/README.md gained a table of all verification scripts; CREDITS.md corrected (three files, 58 segments); docs/HANDOFF.md rewritten for the final state (A–F, D, H done; G in progress; audio + commit outstanding). Perf numbers left for G. Stale Titanic comments remain in src/gl/atlas.ts, scripts/verify-hidpi.mjs, vite.config.ts (not docs).
- 2026-08-27 08:51 (wp1-2): I-cards WP1 (content model, `src/data/card.ts`, dataset declarations) and WP2 (quiet card painter, `compileCard`, group covers) landed. 357 tests. Narration audio generated at 09:22, so the three audio-hash tests are live.
- 2026-08-27 09:38: plan I gained §9, three decisions from the user after seeing the tour with audio: the tour keeps the dense card and its narration (so §7.1 item 4 is cancelled); capacity-fitting must apply to per-item collections too; the zoomed-out map blooms.
- 2026-08-27 10:03 (lead, after a crash): resumed. `vite.config.ts` watcher inverted from a deny-list to an **allow-list** over `src/`, `public/`, `index.html` and the three config files, with a chokidar predicate that prunes at the top level; `scripts/vite.e2e.config.mjs` inherits it. Verified on a scratch server: src edits still reload, 548 -> 483 inotify watches, and the running dev server restarted itself onto the new config.
- 2026-08-27 10:12 (lead): **WP3** landed. `planTier` + `UNIQUE_MIN_PX` + `planReady` + `onScreenCards` in `src/gl/hires.ts`; `hiResTier` deleted (the size-only rule is gone, per §9.2). `buildCards` paints group covers above the per-item cap and `specOf` is always the row; `HIRES_BUDGET` (24 cards) -> `HIRES_PIXEL_BUDGET` (4 Mpx); style uploads batch above 256 rows; the plan commits atomically once every on-screen card has art. `FrameModel` gains `cardTier`/`perItem`; `PivotApp.cardScreenRect`. New `tests/tiers.test.ts`. `scripts/verify-card.mjs` phase 3 is now the uniqueness assertion (two same-topic neighbours differ byte-wise): passes, tier 512, 48 cards.
- 2026-08-27 10:14 (lead): bench. The swiftshader sweep could not finish in 25 minutes and the existing `bench-results/latest.json` baseline is degenerate (p95 = 0 above 10,000 rows), so it was abandoned and a full run captured instead: `bench-results/after-wp3-*.json`. It is still software (llvmpipe, not a GPU) — 60 fps to 1,000, 42-51 at 10,000, 11-18 at 100,000, 2-4 at 500,000, 1,000,000 does not complete. Recorded in README as a floor. **No GPU baseline exists**, so plan I's "zoom p95 must not worsen by 5 %" budget could not be evaluated; the bench's phases never settle the camera, so the hi-res pass provably does not run in them.
- 2026-08-27 10:28 (lead): **WP4** landed. `#detail`/`#toast` moved out of `.stage` into `#overlay` (a sibling of `#app`) with a scrim; the pane is `aria-modal="true"`, makes `#app` inert, traps Tab and closes on the scrim. `src/ui/detail/flip.ts` expands it out of the card's own rect (`app.cardScreenRect`), skipped entirely under reduced motion. `src/ui/detail/template.ts` renders a dataset's `detail` declaration: shared header, summary tiles, sections, and a **Context** block — the honest home of the deleted aggregate card, showing each facet value's share of the *filtered* set. `taxCaseDetail` now composes the shared header/context/actions and keeps its own three sections; the CSS for its timeline, chips and stars was missing entirely and is now written. `.tour` z-index raised to 50 (above `#overlay`), or the modal covered the tour's own controls.
- 2026-08-27 10:30 (lead): tour change from WP4. The `record` step no longer opens the modal — the modal covers the middle of the board, and the narration for that step is about what the *card* says; the `detail` step opens it. The `clear` step closes it first, since `#app` is inert. `cardRect` now delegates to `app.cardScreenRect`. Narration unchanged, so no clip hashes move (§9.1).
- 2026-08-27 10:45 (lead): **WP5** landed. `src/ui/settings.ts` (state + popover): Design (Auto/Simple/Detailed, Detailed disabled where there is no bespoke card), Labels, Tags, Title; persisted under `tessera.cards.v1`; `?cards=` pins the design only. Two bugs found by driving it in a browser: re-rendering the popover dropped focus to `<body>` so Escape stopped working (focus is now restored by index, and Escape is handled on the document), and the top bar's `overflow-x: auto` clipped it (the popover is now viewport-anchored from the button's rect). §9.3 glow fix in the shader: halo floor 6 px -> 2.5 px, amplitude scaled by 1/halo² so a card's total emission is zoom-independent, gaussian tightened to `exp(-6r²)` with the peak raised 1.5x so the *fitted* view is unchanged (3.06x vs 3.08x before) and only the far view changes. §6 usability: hover and keyboard-focus share the spare middle value of the selection byte, `renderer.pick` gated to 200,000 rows and throttled to one per frame, the canvas is `tabindex="0" role="application"` with a described-by help line, arrow keys walk between cards, Enter opens the record, and a live region plus a cursor chip say what a card says when it is too small to draw. New `scripts/verify-cards.mjs` (15 checks).
- 2026-08-27 10:50 (lead): the cursor chip exposed a real degradation bug — at `tax-cases:100000` the title falls back to `Topic`, which is also the eyebrow, so the card read "PAYE PAYE". `compileCard` and `modalHeader` now drop a topic equal to the title; covered in `tests/card-template.test.ts`. `MetricSpec.label` accepts an accessor so "1 contact" agrees with its value.
- 2026-08-27 10:58 (lead): the atomic commit made `verify-hidpi` flaky — it sampled `frame.hiRes` between the flight and the commit, then read pixels after it. Both scripts now wait for the plan to commit before sampling. **WP6**: README, `scripts/README.md`, this log and `docs/HANDOFF.md` brought up to date.
- 2026-08-27 11:20 (lead): user feedback — "the data for the larger datasets need to be regenerated: tax customer service 100,000 is not the same shape as 3,000". It was not: **all five** generators dropped their identity column above 50,000 rows (`tax-cases` also dropped `Customer` and `Postcode` and re-pointed `labelColumn` to `Topic`; `tax-returns` to `Sector`, `payments` to `Merchant category`, `invoices` to `Supplier`, `products` to `Type`), and `tax-cases` switched card design above 3,000. Fixed: `TextColumn` gains an `at(i)` accessor and `values` may be null; `derivedText` (src/data/columnar.ts) makes the five formulaic identity columns computed rather than stored, so a million rows cost nothing; `tax-cases` materialises `Customer` and `Postcode` at every size (faker is now loaded for every tax-cases size — +200 ms at 100k, measured) and declares `card.custom` unconditionally, which WP3 makes correct since every card is per-row art at any size. `toLayoutData` skips text columns (a derived one carries a closure structured clone would reject). Tests that encoded the old gates were rewritten to assert the new invariant. 391 tests.
- 2026-08-27 11:26 (lead): the tour e2e caught a real spotlight bug while re-verifying: `.tour-spot` transitions over 0.35 s, and while it *tracks a card on the canvas* that ease restarts every frame, so the ring trails the card and the caption — placed from the true rect — overlaps it. `.tour-spot.tracking` now has `transition: none`. The e2e's adjacency failure now prints both rects.
